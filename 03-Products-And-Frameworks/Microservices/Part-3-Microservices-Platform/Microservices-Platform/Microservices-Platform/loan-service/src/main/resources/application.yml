server:
  port: 9102
spring:
  application:
    name: loan-service

  config:
    import: optional:configserver:http://localhost:8888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8765/eureka/
management:
  endpoints:
    web:
      exposure:
        include: health,info
